import 'package:flutter/material.dart';

class Testingnav extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Just Check, it\'s work'),
      ),
    );
  }
}
